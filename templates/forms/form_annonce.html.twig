{% extends 'base.html.twig' %}

{% block body %}
{% include "site-parts/slider.html.twig" %}

{{ form_start(annonceForm) }}
<div class="row mt-3">
   <div class="col">
      {{ form_row(annonceForm.nom_event, {'attr' : {placeholder : "Nom de l'évenement", 'class': 'form-control'}, 'label' : false}) }}
   </div>
</div>
       
<div class="row mt-3">
   <div class="col">
      {{ form_row(annonceForm.genre_event, {'attr' : {placeholder : "Genre de l'évenement", 'class': 'form-control'}, 'label' : false}) }}
   </div>
</div>

<div class="row mt-3">
   <div class="col">
      {{ form_row(annonceForm.date_begin, {'attr' : {'class': ''}, 'label' : 'Date de début'}) }}
   </div>
</div>

<div class="row mt-3">
   <div class="col">
      {{ form_row(annonceForm.date_end, {'attr' : {'class': ''}, 'label' : "Date de fin"}) }}
   </div>
</div>
       
<div class="row mt-3">
   <div class="col">
      {{ form_row(annonceForm.description, {'attr' : {placeholder : "Description de l'évenement", 'class': 'form-control'}, 'label' : false}) }}
   </div>
</div>
            
        {{ form_end(annonceForm) }}
        {% endblock %}
    