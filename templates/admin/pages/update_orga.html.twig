{% extends 'base.html.twig' %}

{% block body %}
    <h1>Update</h1>

    {{ form_start(organisateurForm) }}
    <div class="form-group mt-5">
        <div class="row">
            <div class="col-2 mb-4">
                {{ form_row(organisateurForm.name, {'attr': {class : "form-control"}}) }}
            </div>
            <div class="col-12 ">
                {{ form_row(organisateurForm.submit, {'attr' : {class : "btn btn-primary"}}) }}
            </div>
        </div>
    </div>

    {{ form_end(organisateurForm) }}

{% endblock %}