{% extends 'admin_base.html.twig' %}

{% block body %}
    <h1>Update</h1>

    {{ form_start(musicienForm) }}
    <div class="form-group mt-5">
        <div class="row">
            <div class="col-2">
                {{ form_row(organisateurForm.name, {'attr': {class : "form-control"}}) }}
            </div>
            <div class="col-4">
                {{ form_row(organisateurForm.surname, {'attr': {class : "form-control"}}) }}
            </div>
            <div class="col-12">
                {{ form_row(organisateurForm.sex, {'attr': {class : "form-control"}}) }}
            </div>
            <div class="col-12">
                {{ form_row(organisateurForm.groupes, {'attr': {class : "form-control"}}) }}
            </div>
            <div class="col-2">
                {{ form_row(organisateurForm.instruments, {'attr': {class : "form-control"}}) }}
            </div>
            <div class="col-4 mt-5">
                {{ form_row(organisateurForm.ajouter, {'attr' : {class : "btn btn-primary"}}) }}
            </div>
            

        </div>
    </div>

    {{ form_end(musicienForm) }}

{% endblock %}



