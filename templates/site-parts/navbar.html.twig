<nav class="navbar navbar-expand navbar bg-dark">
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="navbar-brand" href="{{ path('home') }}">Home</a>
      </li>
      <li class="nav-item">
        <a class="navbar-brand" href="{{ path('user_home') }}">Ma page</a>
      </li>
      {% if app.user and app.user.roles[0] == "ROLE_GROUPE" %}
      <li class="nav-item">
        <a class="navbar-brand" href="{{ path('search_ann') }}">Annonces</a>
      </li>
      {% endif %}
      <li class="nav-item">
        <a class="navbar-brand" href="{{ path('agenda' , {from : 1})  }}" >Agenda</a>
      </li>
      <li class="nav-item">
        {% if app.user %}
        <a class="navbar-brand" href="{{ path('logout') }}" >Se déconnecter</a>
        {% else %}
          <a class="navbar-brand" href="{{ path('login') }}" >Se connecter</a>
        {% endif %}
        
      </li>
      
    </ul>
  </div>
</nav>